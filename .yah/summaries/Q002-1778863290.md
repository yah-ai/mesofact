---
ticket: Q002
author: agent:claude
timestamp: 1778863290
promoted: false
---

Refined `.yah/docs/working/mesofact.md` into the board.

**Quest**
- Q002 — mesofact MVP rollout (coordinates the 10 phase relays)

**Phase relays** (all bare-R children of Q002, all in Open):
- R003 — P1: Workspace skeleton + render contract types  *(no deps — start here)*
- R004 — P2: Manifest schema + route config + build-time validation  *(deps: R003)*
- R005 — P3: Bun render-pool worker (IPC + lifecycle)  *(deps: R003)*
- R006 — P4: Adapter API + r2 adapter (read-only)  *(deps: R005)*
- R007 — P5: Build pipeline (TS build → manifest → Mode 1 prerender)  *(deps: R004, R005, R006)*
- R008 — P6: Publisher (R2 upload + manifest swap + CDN purge)  *(deps: R006, R007)*
- R009 — P7: Rust proxy (axum) — boot, manifest reload, Mode 1 fallback  *(deps: R005, R007, R008)*
- R010 — P8: yah.dev marketing page (Mode 1 dogfood)  *(deps: R007, R008, R009)*
- R011 — P9: Mode 2 SSR slice (sqlite + response cache + session)  *(deps: R006, R009)*
- R012 — P10: Mode 3 SPA hydration + observability MVP  *(deps: R007, R009, R011)*

**P1 sub-tickets** (under R003, opened now since P1 is ready to start):
- R003-T1 — Cargo workspace bootstrap
- R003-T2 — Scaffold @mesofact/runtime package  *(deps: T1)*
- R003-T3 — Define render contract types  *(deps: T2)*
- R003-T4 — examples/yah-dev placeholder  *(deps: T2)*
- R003-T5 — Outside-consumer smoke test  *(deps: T3)*

**Not yet refined**: P2–P10 sub-tickets. Their candidate-ticket lists are still in the doc — re-run `/refine` (or `board open --kind task --parent R0XX`) once a phase is the next thing being claimed and the code structure is concrete enough that ticket boundaries won't churn.

**Out of MVP scope** (deliberately not refined): pg adapter, rpc adapter, Litestream/LiteFS choice, long-lived publisher daemon, JWT/OAuth sessions, multi-tenancy, per-adapter OTLP spans, multi-region pools, `mesofact-publisher reconcile`. See "Out of MVP scope" section of the rollout doc.

**Coverage stamp**: `<!-- @yah:covered-by(Q002, status=active, 2026-05-15) -->` added above the H1 in the rollout doc so future `/refine` runs detect coverage even after relays move to review.

**To start P1**: `yah board claim R003-T1` (workspace bootstrap is the first deps-clear sub-ticket).
